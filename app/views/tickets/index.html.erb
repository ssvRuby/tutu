<h1>Список билетов</h1>
<table>
  <thead>
  <th>Номер</th>
  <th>Пассажир</th>
  <th>Пасспорт</th>
  <th>Поезд №</th>
  <th>Станция отпр.</th>
  <th>Станция назн.</th>
  <th>Дата приобретения</th>
  </thead>
  <tbody>
  <%@tickets.each do |t| %>
  <tr>
    <td><%=t.number%></td>
    <td><%=t.passenger_name%></td>
    <td><%=t.passport_number%></td>
    <td><%=t.train.number%></td>
    <td><%=t.start_station.title%></td>
    <td><%=t.end_station.title%></td>
    <td><%=t.created_at%></td>
    <td><%=link_to 'Показать', ticket_path(t)%></td>
    <td><%=link_to 'Удалить', ticket_path(t), method: :delete, data: { confirm: 'Вы уверены?' } %></td>
  </tr>
  <%end%>
  </tbody>
</table>
<p>
  <%= link_to 'Новый поиск', search_path %>
</p>